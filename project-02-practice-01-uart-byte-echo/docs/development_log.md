# Project 02 Practice 01 UART Byte Echo Development Logs

# Checkpoint 1

Firts i create two variable. These variables are as follow: `rx_msg_byte` and `rx_msg_ready`.
When any key is pressed, the key's value is send to the STM32 via UART.
This value is saving `rx_msg_byte` and this value send to computer via UART
